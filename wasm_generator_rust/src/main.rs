fn main() {
    // let mut file = File::create("abi.json").expect("Unable to create file");
    // file.write_all(erc20_functions::ABI.as_bytes()).expect("Unable to write data");
    //let _token = ERC20Token::new("TokenName".into(), "TKN".into(), 18, 1000);
    println!("Hello from WASM GENERATOR RUST!");
}
